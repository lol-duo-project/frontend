import Style, { generateClassNames } from "@src/utils/Style"
import { TextButton } from "../Button"

export const TabBar = () => {
  return (
    <Style css={css}>
      <TextButton>ㄴㄴ</TextButton>
    </Style>
  )
}

const CLASSNAMES = generateClassNames({

})

const css = `
  
`